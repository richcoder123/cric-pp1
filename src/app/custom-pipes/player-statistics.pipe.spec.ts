import { PlayerStatisticsPipe } from './player-statistics.pipe';

describe('PlayerStatisticsPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerStatisticsPipe();
    expect(pipe).toBeTruthy();
  });
});
